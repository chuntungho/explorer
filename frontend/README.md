# Development

> Install package tool: `npm install -g pnpm@latest-10`

## Setup
1. Install dependencies: `pnpm install`
2. Start development server: `pnpm dev`
3. Open `http://localhost:3000` in your browser to view the application.   